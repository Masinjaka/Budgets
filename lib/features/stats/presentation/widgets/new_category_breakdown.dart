import 'package:flutter/material.dart';

class NewCategoryBreakdown extends StatelessWidget {
  const NewCategoryBreakdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
